type: object
required:
  - fileId
  - fileName
  - title
properties:
  fileId:
    type: string
    format: uuid
    description: The unique identifier for the indexed file
  fileName:
    type: string
    description: The name of the indexed file
  title:
    type: string
    description: The title of the indexed file, used in search results
  uploadDate:
    type: string
    format: date-time
    description: The date and time when the file was uploaded
  embedded:
    type: boolean
    description: Indicates whether the file is embedded in the index. If true, the file content is indexed and searchable.
additionalProperties: true
